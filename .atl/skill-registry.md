# Skill Registry — micha

Auto-generated by sdd-init. Cached for sub-agent injection.

## Project Conventions

| File | Type | Path |
|------|------|------|
| AGENTS.md | Index | `/home/ale/Documents/Projects/micha/AGENTS.md` |
| copilot-instructions.md | Convention | `/home/ale/Documents/Projects/micha/.github/copilot-instructions.md` |
| go.instructions.md | Convention | `/home/ale/Documents/Projects/micha/.github/instructions/go.instructions.md` |

## User Skills

| Skill | Trigger | Path |
|-------|---------|------|
| go-testing | Go tests, Bubbletea TUI testing | `/home/ale/.config/opencode/skills/go-testing/SKILL.md` |
| golang-testing | Writing tests for Go code, TDD workflow | `/home/ale/.agents/skills/golang-testing/SKILL.md` |
| golang-patterns | Writing new Go code, idiomatic patterns | `/home/ale/.agents/skills/golang-patterns/SKILL.md` |
| code-reviewer | PR review, code quality, refactoring | `/home/ale/.agents/skills/code-reviewer/SKILL.md` |

## Compact Rules

### go-testing

**Context**: `**/*.go`, `**/*_test.go`

**Triggers**: Writing Go tests, using teatest, adding test coverage

**Rules**:
- Use table-driven tests with descriptive names
- Structure: `tests := []struct { name string; input X; expected Y; wantErr bool }`
- Always use `t.Run(tt.name, func(t *testing.T) {...})` for subtests
- For TUI testing: test Model.Update() directly for state changes
- Use teatest.NewTestModel() for full interactive flows
- Golden file testing for visual output validation
- Mock dependencies with interfaces, use t.TempDir() for file operations
- Test commands: `go test ./...`, `go test -cover`, `go test -v`, `go test -short`

### golang-testing

**Context**: `**/*.go`, `**/*_test.go`

**Triggers**: Writing tests for Go code, setting up TDD workflow, improving test coverage

**Rules**:
- Follow RED-GREEN-REFACTOR cycle: failing test → minimal code → refactor
- Use table-driven tests as the default pattern
- Use testify/assert and testify/require for assertions
- Structure test files alongside production code (`foo_test.go`)
- Use subtests with t.Run() for test cases
- Mock outbound ports with hand-written fakes or testify/mock
- Test edge cases: empty input, nil, zero values, boundary conditions
- Use t.Parallel() when tests are independent
- Benchmark with `func BenchmarkXxx(b *testing.B)` and `-bench` flag
- Use fuzzing for input validation: `func FuzzXxx(f *testing.F)`

### golang-patterns

**Context**: `**/*.go`

**Triggers**: Writing new Go code, reviewing Go code, refactoring

**Rules**:
- Keep code simple and clear — avoid cleverness
- Make the zero value useful (design types ready to use without init)
- Use guard clauses — return early on error, avoid nested ifs
- Wrap errors with context: `fmt.Errorf("operation: %w", err)`
- Prefer interfaces over concrete types for flexibility
- Accept interfaces, return structs
- Use functional options pattern for constructors with many params
- Embed for composition, not inheritance
- Use context.Context as first argument for cancellation/timeouts
- Avoid goroutine leaks — always ensure goroutines can exit
- Use sync.WaitGroup or errgroup for goroutine synchronization
- Prefer channels for communication, mutexes for protecting state

### code-reviewer

**Context**: Any code files during review phase

**Triggers**: PR review, code quality assessment, refactoring suggestions

**Rules**:
- Prioritize: Correctness → Architecture → Security → Reliability → Performance → Maintainability → Testing
- Check diff hygiene: changes minimal and focused?
- Verify architecture alignment: layering, dependency direction, boundaries
- Security: no hardcoded secrets, proper authz/authn, input validation
- Error handling: all errors handled, wrapped with context, logged appropriately
- Performance: check big-O, avoid N+1 queries, unnecessary allocations
- Testing: meaningful tests for failure modes and edge cases
- Maintainability: clear naming, no duplication, functions < 50 lines
- Provide concrete suggestions with code examples
- Classify feedback: CRITICAL (blocks merge), WARNING (should fix), SUGGESTION (optional)

## Referenced Convention Files

### AGENTS.md

**Type**: Index file

**Content**:
- Layer map: domain → application → ports → adapters → cmd
- Feature order: domain entity → use case → port interfaces → adapter → wiring
- Domain conventions: rich constructors, unexported fields, *Attributes for DB rehydration
- Request flow: route → handler → use case → domain → repo
- Build & test: `go run ./cmd/api`, `go test ./...`, `go test -race ./...`

### copilot-instructions.md

**Type**: Project conventions

**Content**:
- Architecture: DDD + Clean Architecture + Hexagonal
- Layer dependency rules (inward only)
- Domain conventions: rich constructors, never ad-hoc struct literals
- Request flow template for new endpoints
- PostgreSQL connection: localhost:5432, db/user/pass: micha/micha/micha_dev_password

### go.instructions.md

**Type**: Language-specific conventions

**Content**:
- Error handling: always wrap with context, use guard clauses
- Logging: use slog with structured fields, propagate context.Context
- HTTP handlers: MaxBytesReader, DisallowUnknownFields, dedicated request structs
- Database: parameterised queries only, scan into *Attributes, wrap errors
- Testing: table-driven with testify, subtests, mock outbound ports
- Domain layer: no infrastructure imports, validation in constructors, typed errors
